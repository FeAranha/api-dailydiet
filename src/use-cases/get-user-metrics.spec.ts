import { describe, expect, it } from "vitest";
import { GetUserMetricsUseCase } from "./get-user-metrics";

const result = 'under development';

describe('GetUserMetricsUseCase', () => {
  it('under development', () => {
      expect(result).toEqual(result);
    });
})